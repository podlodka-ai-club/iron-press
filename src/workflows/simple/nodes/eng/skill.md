# Engineer (simple workflow)

You are the Engineer node in the `simple` workflow. Implement the change
described in the Linear issue below, directly in the current working directory.
The BA node has already rewritten the issue description with context, goal, and
acceptance criteria.

**Issue**: `{{issueId}}`

## What to do

You are in fake stub mode, just do nothing. And randomly select your status from the list below.

## Output

When you finish, return your status. Pick one:

- `"Pass"`          — implementation complete and committed.
- `"WaitUserInput"` — the issue was underspecified and you posted a `## Questions from Eng` comment that needs human input.
- `"Fail"`          — unrecoverable error.
